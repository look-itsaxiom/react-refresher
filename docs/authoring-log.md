# Authoring log

Append-only record of autonomous authoring runs (see `docs/authoring-runbook.md`).

## Blocked

(none)

## Runs

- 2026-09-17 17:35 PDT — Curriculum expanded to 101 lessons across 21 tracks (roadmap.sh/frontend, 2026 edition). 3 authored, 98 pending. Scheduled runs: every 30 minutes from ~19:42 PDT through 08:42 PDT on 2026-09-18, 4 lessons per run.
- 2026-09-17 20:05 PDT — Run 1: added 04-effects-and-refs, 05-context-and-composition, 06-custom-hooks, 07-lists-keys-forms (Refresher track complete). Blocked: none. Pending: 94. Suite 118/118, typecheck clean. Notes: implementers flagged two facts stated from knowledge rather than re-fetched this run: React 19 form actions resetting uncontrolled fields (covered by lesson 03's passing checks, so verified behaviorally) and ref-callback cleanup / ref-as-prop without forwardRef (well documented in the React 19 release post). Also gitignored Claude Code's `.claude/scheduled_tasks.lock`.
- 2026-09-17 20:45 PDT — Run 2: added 08-concurrent-rendering, 09-external-stores, 10-use-and-ref-changes, 11-metadata-and-resources (React 18 track complete). Blocked: none. Pending: 90. Suite 142/142, typecheck clean. Notes: lesson 11 found and teaches two real gotchas (React only hoists a `<title>` with a single string child; react-dom's preload registry is global per document). Unverified-by-fetch this run: Next.js `metadata` / React Router 8 `meta` API details; exact version that added `useDeferredValue`'s initialValue (text avoids a version claim). Added `.gitattributes` (LF) to stop CRLF warnings.
- 2026-09-17 21:12 PDT — Run 3: added 12-react-19-removals, 24-activity-effect-events-view-transitions, 13-what-the-compiler-does, 14-compiler-friendly-code (React 19 and Compiler tracks complete). Blocked: none. Pending: 86. Suite 166/166, typecheck clean. Notes: lesson 24 verified Activity/ViewTransition/addTransitionType/useEffectEvent against the installed react@19.3.0 and probed Activity's hidden mode in jsdom (display:none, effects unmounted, state kept). Lesson 13 flags that "plugin-react compiler:true uses oxc-transform-react" is inferred from this repo's config, not react.dev; lesson 12 notes forwardRef is deprecated-not-removed as of 19.3. Two exercises grade a named export (`mod.Leaderboard`) because the entry component takes props.
- 2026-09-17 21:42 PDT — Run 4: added 15-server-state-tanstack-query, 16-client-state-zustand, 17-forms-and-validation, 18-styling-in-2026. Blocked: none. Pending: 82. Suite 190/190 on two consecutive runs, typecheck clean. Notes: the sandbox cannot import TanStack Query, Zustand, Zod, or Tailwind, so these lessons teach the real libraries in prose and grade teaching miniatures (mini useQuery/useMutation, mini create() store, a Standard-Schema-shaped validate(), a cva-style variant builder); prompts say so. Two authors saw transient failures while lesson 15 was mid-edit (shared fake-server state across concurrent test runs); none reproduce once files settled. Unverified-by-fetch: Zod 4 / ArkType perf claims are vendor-stated; State of React 2025 percentages via search summaries; several TanStack Query v5 API details from prior knowledge.
- 2026-09-17 22:12 PDT — Run 5: added 20-typescript-react-idioms, 21-server-components, 22-server-functions, 23-frameworks (Ecosystem and Server-side React tracks complete; the whole React path, 24 lessons, is now authored). Blocked: none. Pending: 78. Suite 214/214, typecheck clean. Notes: server-track exercises are explicit simulations (RSC serialization rules, a mini Flight payload renderer, a hardened server action against `@server/todos`, a tag-based cache, App Router file routing, parallel loaders). Unverified-by-fetch: several TypeScript 7 / tsgo specifics in lesson 20 (written from prior knowledge under the time budget; hedged); CVE-2025-55182 details via search summaries; Next.js 16 `revalidateTag` second-argument requirement hedged; React Router 8 RSC flag name not cited.
- 2026-09-17 22:40 PDT — Run 6: added 25-how-the-web-works, 26-how-browsers-render, 27-html-that-matters, 28-modern-css. Blocked: none. Pending: 74. Suite 238/238 on two consecutive runs, typecheck clean. Notes: jsdom 30 facts probed by authors: `dialog.show/showModal/close` are not callable (only `.open` reflects) and `<details name>` exclusivity is unimplemented, so lesson 27 grades `.open` and says so; `requestAnimationFrame` does work, so lesson 26 uses the real API. Unverified-by-fetch: HTTP/3 and ECH adoption figures (hedged ranges); several CSS Baseline dates in lesson 28 from prior knowledge (fast-moving features were live-checked); `closedby` and customizable `<select>` support snapshots from aggregators.
- 2026-09-17 23:10 PDT — Run 7: added 29-web-apis-fetch-streams-workers, 30-web-apis-storage-observers, 31-modern-javascript, 32-event-loop-and-async (Web platform track complete). Blocked: none. Pending: 70. Suite 262/262 on two consecutive runs, typecheck clean. Notes: controller raised tsconfig `lib` from ES2023 to ESNext (TS 7) so ES2024/2025 APIs (Object.groupBy, Promise.withResolvers, Set methods, Iterator helpers) type-check without a lesson-local ambient shim; lesson 31's `lib-shims.d.ts` was removed. jsdom facts: Web Workers, IndexedDB, IntersectionObserver absent (lessons ship fakes); MessageChannel and streams present. Gotcha taught in lesson 31: `Iterator.prototype.take(n).toArray()` closes the source iterator, so repeated paging must pull with `.next()`. Unverified-by-fetch: Navigation API/OPFS support snapshots (single search summary); Temporal per-engine ship versions; several long-stable Fetch/Worker facts from prior knowledge.
- 2026-09-17 23:45 PDT — Run 8: added 33-modules-and-import-maps, 34-typescript-advanced, 35-package-managers, 36-module-bundlers-concepts (JavaScript/TypeScript track complete). Blocked: none. Pending: 66. Suite 286/286, typecheck clean. Notes: one flake seen in a previously committed lesson (15 mini-use-mutation, `waitFor` 800ms windows too tight under a 47s suite); controller widened those windows to 2000ms and gave every exercise validation test a 30s budget in `solutions.test.ts` (a starter that fails several `waitFor`s legitimately takes longer than Vitest's 5s default). Lesson 34 uses `Expect<Equal<>>` and `@ts-expect-error` lines in solutions as type-level tests enforced by `pnpm typecheck`. Unverified-by-fetch: TS 7 perf multipliers (vendor figures); exact npm/Yarn minors; import-map scope keys use absolute URLs by design (stated in prompt).
- 2026-09-18 00:30 PDT — Run 9: added 37-bundler-landscape, 38-linters-and-formatters, 39-monorepos, 19-testing-in-2026 (Tooling track complete). Blocked: none. Pending: 62. Suite 310/310, typecheck clean. Notes: the 00:12 fire at 00:20 (run 10) was skipped because run 9's lessons were still in progress in the working tree (preflight rule); runs now take ~30+ min with 4 lessons, so alternate fires may be skipped. Verified 2026 facts worth knowing: TypeScript 7.0 shipped without a public compiler API, so typescript-eslint 8 cannot run type-aware rules on it until TS 7.1; oxlint's tsgolint works around that. Unverified-by-fetch: exact Rolldown 1.0 / Vite 8 GA months; tool satisfaction/perf percentages in lesson 19 (search summaries); Farm bundler omitted.
- 2026-09-18 01:15 PDT — Run 10: added 40-vitest-deep-dive, 41-testing-library-philosophy, 42-playwright-e2e, 43-api-mocking-and-storybook (Testing track complete). Blocked: none. Pending: 58. Suite 334/334 on two consecutive runs, typecheck clean. Notes: one author saw a single load-dependent flake in a `@server/todos` check (lessons 03/15) while four authors ran suites concurrently; it did not reproduce in either verification run. Exercises are miniatures of the tools (mini test runner + mock/fake clock, a role-query resolver graded against real Testing Library, a locator engine + auto-wait on a fake clock, a mini MSW router + CSF3 composeStories). Verified: Storybook decorator order global → meta → story; Vitest 5 `clearMocks` default. Unverified-by-fetch: Vitest Browser Mode locator API details; Playwright version/MCP maturity; MSW WebSocket/GraphQL surface details.
- 2026-09-18 01:50 PDT — Run 11: added 44-rendering-strategies, 45-hydration, 46-ssr-in-practice, 47-static-site-generators. Blocked: none. Pending: 54. Suite 358/358, typecheck clean. Notes: controller exposed `react-dom/server` in the sandbox registry so SSR/hydration exercises render real HTML with `renderToString` and hydrate it, counting mismatches via `hydrateRoot`'s `onRecoverableError`. Facts established by probing React 19.3 in jsdom: attribute mismatches are silently ignored (server value wins, no recoverable error); text mismatches trigger one recoverable error and regenerate the node; SSR inserts `<!-- -->` between adjacent text children; 19's extension tolerance is scoped to unexpected tags in head/body. Language gotchas taught: `JSON.stringify` calls `Date#toJSON` before replacers run; regex literals still reject raw U+2028/2029. Removed a stray probe file (`scratch-backslash-test.txt`) an author left at the repo root. Unverified-by-fetch: Vite 8 Environment API surface; Next.js 16 Cache Components naming; Astro server islands wiring; Pagefind ubiquity.
- 2026-09-18 02:25 PDT — Run 12: added 48-choosing-a-framework, 49-core-web-vitals, 50-loading-performance, 51-images-and-media (Rendering strategies track complete). Blocked: none. Pending: 50. Suite 382/382, typecheck clean. Notes: dynamic `import()` of sibling learner files works in the sandbox, so lesson 50 grades real `React.lazy` + Suspense code splitting with hover preloading. INP's discard-one-per-50 rule was verified against the web-vitals library source. Unverified-by-fetch: State of JS 2025 percentages (charts only); TanStack Start RC/stable status; Soft Navigations API rollout details; Speculation Rules coverage figures; JPEG XL and `sizes="auto"` support snapshots (secondary sources).
- 2026-09-18 02:55 PDT — Run 13: added 52-caching-strategies, 53-runtime-performance, 54-react-performance, 55-accessibility-foundations (Performance track complete). Blocked: none. Pending: 46. Suite 406/406, typecheck clean. Notes: verified by probing React 19.3 in jsdom: a hidden `<Activity>` subtree never runs passive effects until it becomes visible (and re-runs them on each hide→show), but lazy `useState` initializers do run while hidden; lesson 54's pre-render exercise and prose were corrected accordingly (the controller's brief had assumed effects pre-run). Verified: Chrome's bfcache now works with `Cache-Control: no-store` (rollout completed April 2025, with limits). Unverified-by-fetch: Section 508 / EN 301 549 WCAG-version alignment and current ADA Title II deadlines (hedged in text); React 19.2 Performance Tracks subtrack details (secondary sources); Compression Dictionary Transport omitted.
- 2026-09-18 03:25 PDT — Run 14: added 56-semantic-html-and-landmarks, 57-aria-done-right, 58-keyboard-and-focus, 59-accessible-forms-and-motion. Blocked: none. Pending: 42. Suite 430/430, typecheck clean. Notes: lesson 59 dropped a controller-suggested check (`getByRole('listitem', { name })`) because ARIA prohibits accessible names on list items, grading the same requirement by text pairing instead. Several authors ran out of web-search budget this run and hedged bleeding-edge facts in text rather than asserting them: native `<dialog>` inert/focus-restoration specifics, Popover API baseline timing, `closedby`/invoker commands, `aria-errormessage` and `:user-invalid` AT support, APG maintenance status, Safari `list-style: none` behavior.
- 2026-09-18 03:55 PDT — Run 15: added 60-testing-accessibility, 61-accessible-react-components, 62-frontend-threat-model, 63-xss-and-sanitization (Accessibility track complete; Security track started). Blocked: none. Pending: 38. Suite 454/454 on two consecutive runs, typecheck clean. Notes: the session's web-search budget was exhausted this run, so several facts are hedged in text rather than asserted: OWASP Top 10:2025 category names, CVE-2025-55182 and Sept 2025 npm-worm specifics (both consistent with the earlier verified research report), Deque's ~57% automated-coverage figure, Guidepup maturity, Base UI/Radix status (shadcn moved to Base UI July 2026 per the research report), customizable `<select>` support, Sanitizer API/DOMPurify details. MDN fetches did confirm Trusted Types Baseline (Feb 2026) and Sanitizer API "limited availability". One transient failure in committed lesson 58 was seen during four concurrent test runs; it did not reproduce in either verification run. jsdom fact: Testing Library computes an empty name for `hidden` elements even with `{ hidden: true }`, so hidden panels are located via `aria-controls` ids.
- 2026-09-18 04:30 PDT — Run 16: added 64-content-security-policy, 65-cors-explained, 66-csrf-clickjacking-isolation, 67-https-and-headers. Blocked: none. Pending: 34. Suite 478/478 after hardening, typecheck clean. Notes: lesson 15's mini-use-mutation flaked a third time under load (the validation file now runs ~78s); controller hardened its two race points (first check's server latency 300→3000ms since it only asserts pre-response state; third check now lets the initial load finish at latency 0 before arming `failNext`). Search budget remained exhausted; authors verified via direct MDN/Chrome/web.dev/Vite/Next.js fetches where possible. Hedged in text: CA/B Forum certificate-lifetime schedule, Chrome HTTPS-First default status, Lax+POST mitigation status, third-party-cookie reversal wording, React Router 8 origin-check specifics, ECH/post-quantum TLS adoption. Lesson 67 grades a real SRI hash via `crypto.subtle`.
- 2026-09-18 05:05 PDT — Run 17: added 68-supply-chain-security, 69-sessions-vs-tokens, 70-cookies-and-token-storage, 71-jwt-in-depth (Security track complete; Auth track started). Blocked: none. Pending: 30. Suite 503/503 with `pnpm test` exit 0, typecheck clean. Notes: found and fixed a sandbox bug that explained the recurring lesson 15 flakes and an unhandled rejection that had started failing the Vitest run: fake-server calls still in flight when the runner resets between checks could consume the next check's `failNext` and throw into a dead consumer. `serverCall` now tags calls with a reset generation; stale calls still settle (React 19 entangles in-flight transitions, so a never-settling promise stalls later form actions, verified empirically) but can no longer consume a later `failNext`. Regression test added in core.test.ts. Verified from react.dev's advisory: the December 2025 RSC RCE is CVE-2025-55182 (react-server-dom-*), with CVE-2025-55183/55184/67779 and CVE-2026-23864 as follow-ups; corrected lesson 68 and the research report, which had used the Next.js-side identifier. Verified: IETF browser-based-apps guidance is now RFC 10017 (Aug 2026), Lucia deprecated March 2025. Hedged: tj-actions incident details, Bun release-age equivalent, Auth.js/Clerk/Better Auth session details, RFC citation freshness in lesson 71, Chrome cookie-count limits.
- 2026-09-18 05:35 PDT — Run 18: added 72-oauth-and-oidc, 73-passkeys-and-sso, 74-auth-in-react-apps, 75-graphql-fundamentals (Auth track complete; GraphQL track started). Blocked: none. Pending: 26. Suite 527/527 with `pnpm test` exit 0, typecheck clean. Notes: lesson 72 grades PKCE against the RFC 7636 test vector and confirmed OAuth 2.1 draft -16 (2026-09-03) and RFC 9700 via datatracker; lesson 74 cites the Next.js middleware bypass CVE-2025-29927 (existence confirmed via authjs.dev, exact header/version details hedged). Web search remained exhausted; hedged: passkey adoption numbers, secondary OAuth RFC details and provider quirks, Clerk/Better Auth/Supabase/WorkOS feature specifics, GraphQL-over-HTTP and `@defer`/`@stream` status (left out). TS 7 quirk: `crypto.getRandomValues` needs an explicit `Uint8Array<ArrayBuffer>` under the stricter typed-array generics.
- 2026-09-18 06:00 PDT — Run 19: added 76-graphql-operations, 77-graphql-clients, 78-graphql-typescript-codegen, 79-graphql-vs-rest-vs-trpc (GraphQL track complete). Blocked: none. Pending: 22. Suite 551/551 with `pnpm test` exit 0, typecheck clean. Notes: the GraphQL track is a from-scratch miniature stack (SDL parser, executor with null propagation, fragment/directive expansion, Relay connections, normalized cache with optimistic rollback, TS codegen, DataLoader batching, query cost). Relay connection shape verified via relay.dev; web search remained exhausted. Hedged: `@defer`/`@stream` and `@cost` RFC status, Apollo Client 4 / urql RSC integration specifics, tRPC v11 details, State of JS 2025 GraphQL numbers; lesson 78's concept prose was not re-verified against primary sources this run.
- 2026-09-18 06:20 PDT — Run 20: added 80-custom-elements, 81-shadow-dom-and-slots, 82-lit-and-web-component-libraries, 83-web-components-with-react (Web Components track complete). Blocked: none. Pending: 18. Suite 575/575 with `pnpm test` exit 0, typecheck clean. Notes: the custom element registry is global and permanent, and starter/solution/checks all evaluate in one jsdom process, so every web-component module exports `define(suffix)` and checks register a unique tag per evaluation; documented in prompts and hints. jsdom facts probed: constructors may not set attributes on `this` (NotSupportedError; init moved to `connectedCallback`), `attributeChangedCallback` re-fires for same-value sets, `.part` IDL reflection and declarative Shadow DOM parsing are absent (lesson 81 ships a manual hydrator), constructable stylesheets are present, and monkey-patching `removeEventListener` misses `AbortController`-based removal (lesson 80 grades behaviorally instead). React 19.3 probed: custom-element `on*` props bind the exact case-sensitive remainder as the event type (`onrating-change` catches `rating-change`; `onXChange` does not catch `x-change`); property-vs-attribute is decided by `in` on the instance; `Children.map` prefixes explicit keys with `.$`. Web search remained exhausted; hedged: ElementInternals/form-associated Baseline dates, scoped custom element registries status, Reference Target proposal status, `:host-context()` deprecation, Lit version beyond 3.x, Shoelace→Web Awesome / Material Web / FAST maintenance status, DSD-plus-hydration limits, `@types/react` 19 IntrinsicElements augmentation syntax.
