# Authoring log

Append-only record of autonomous authoring runs (see `docs/authoring-runbook.md`).

## Blocked

(none)

## Runs

- 2026-09-17 17:35 PDT — Curriculum expanded to 101 lessons across 21 tracks (roadmap.sh/frontend, 2026 edition). 3 authored, 98 pending. Scheduled runs: every 30 minutes from ~19:42 PDT through 08:42 PDT on 2026-09-18, 4 lessons per run.
- 2026-09-17 20:05 PDT — Run 1: added 04-effects-and-refs, 05-context-and-composition, 06-custom-hooks, 07-lists-keys-forms (Refresher track complete). Blocked: none. Pending: 94. Suite 118/118, typecheck clean. Notes: implementers flagged two facts stated from knowledge rather than re-fetched this run: React 19 form actions resetting uncontrolled fields (covered by lesson 03's passing checks, so verified behaviorally) and ref-callback cleanup / ref-as-prop without forwardRef (well documented in the React 19 release post). Also gitignored Claude Code's `.claude/scheduled_tasks.lock`.
- 2026-09-17 20:45 PDT — Run 2: added 08-concurrent-rendering, 09-external-stores, 10-use-and-ref-changes, 11-metadata-and-resources (React 18 track complete). Blocked: none. Pending: 90. Suite 142/142, typecheck clean. Notes: lesson 11 found and teaches two real gotchas (React only hoists a `<title>` with a single string child; react-dom's preload registry is global per document). Unverified-by-fetch this run: Next.js `metadata` / React Router 8 `meta` API details; exact version that added `useDeferredValue`'s initialValue (text avoids a version claim). Added `.gitattributes` (LF) to stop CRLF warnings.
- 2026-09-17 21:12 PDT — Run 3: added 12-react-19-removals, 24-activity-effect-events-view-transitions, 13-what-the-compiler-does, 14-compiler-friendly-code (React 19 and Compiler tracks complete). Blocked: none. Pending: 86. Suite 166/166, typecheck clean. Notes: lesson 24 verified Activity/ViewTransition/addTransitionType/useEffectEvent against the installed react@19.3.0 and probed Activity's hidden mode in jsdom (display:none, effects unmounted, state kept). Lesson 13 flags that "plugin-react compiler:true uses oxc-transform-react" is inferred from this repo's config, not react.dev; lesson 12 notes forwardRef is deprecated-not-removed as of 19.3. Two exercises grade a named export (`mod.Leaderboard`) because the entry component takes props.
- 2026-09-17 21:42 PDT — Run 4: added 15-server-state-tanstack-query, 16-client-state-zustand, 17-forms-and-validation, 18-styling-in-2026. Blocked: none. Pending: 82. Suite 190/190 on two consecutive runs, typecheck clean. Notes: the sandbox cannot import TanStack Query, Zustand, Zod, or Tailwind, so these lessons teach the real libraries in prose and grade teaching miniatures (mini useQuery/useMutation, mini create() store, a Standard-Schema-shaped validate(), a cva-style variant builder); prompts say so. Two authors saw transient failures while lesson 15 was mid-edit (shared fake-server state across concurrent test runs); none reproduce once files settled. Unverified-by-fetch: Zod 4 / ArkType perf claims are vendor-stated; State of React 2025 percentages via search summaries; several TanStack Query v5 API details from prior knowledge.
- 2026-09-17 22:12 PDT — Run 5: added 20-typescript-react-idioms, 21-server-components, 22-server-functions, 23-frameworks (Ecosystem and Server-side React tracks complete; the whole React path, 24 lessons, is now authored). Blocked: none. Pending: 78. Suite 214/214, typecheck clean. Notes: server-track exercises are explicit simulations (RSC serialization rules, a mini Flight payload renderer, a hardened server action against `@server/todos`, a tag-based cache, App Router file routing, parallel loaders). Unverified-by-fetch: several TypeScript 7 / tsgo specifics in lesson 20 (written from prior knowledge under the time budget; hedged); CVE-2025-55182 details via search summaries; Next.js 16 `revalidateTag` second-argument requirement hedged; React Router 8 RSC flag name not cited.
- 2026-09-17 22:40 PDT — Run 6: added 25-how-the-web-works, 26-how-browsers-render, 27-html-that-matters, 28-modern-css. Blocked: none. Pending: 74. Suite 238/238 on two consecutive runs, typecheck clean. Notes: jsdom 30 facts probed by authors: `dialog.show/showModal/close` are not callable (only `.open` reflects) and `<details name>` exclusivity is unimplemented, so lesson 27 grades `.open` and says so; `requestAnimationFrame` does work, so lesson 26 uses the real API. Unverified-by-fetch: HTTP/3 and ECH adoption figures (hedged ranges); several CSS Baseline dates in lesson 28 from prior knowledge (fast-moving features were live-checked); `closedby` and customizable `<select>` support snapshots from aggregators.
- 2026-09-17 23:10 PDT — Run 7: added 29-web-apis-fetch-streams-workers, 30-web-apis-storage-observers, 31-modern-javascript, 32-event-loop-and-async (Web platform track complete). Blocked: none. Pending: 70. Suite 262/262 on two consecutive runs, typecheck clean. Notes: controller raised tsconfig `lib` from ES2023 to ESNext (TS 7) so ES2024/2025 APIs (Object.groupBy, Promise.withResolvers, Set methods, Iterator helpers) type-check without a lesson-local ambient shim; lesson 31's `lib-shims.d.ts` was removed. jsdom facts: Web Workers, IndexedDB, IntersectionObserver absent (lessons ship fakes); MessageChannel and streams present. Gotcha taught in lesson 31: `Iterator.prototype.take(n).toArray()` closes the source iterator, so repeated paging must pull with `.next()`. Unverified-by-fetch: Navigation API/OPFS support snapshots (single search summary); Temporal per-engine ship versions; several long-stable Fetch/Worker facts from prior knowledge.
- 2026-09-17 23:45 PDT — Run 8: added 33-modules-and-import-maps, 34-typescript-advanced, 35-package-managers, 36-module-bundlers-concepts (JavaScript/TypeScript track complete). Blocked: none. Pending: 66. Suite 286/286, typecheck clean. Notes: one flake seen in a previously committed lesson (15 mini-use-mutation, `waitFor` 800ms windows too tight under a 47s suite); controller widened those windows to 2000ms and gave every exercise validation test a 30s budget in `solutions.test.ts` (a starter that fails several `waitFor`s legitimately takes longer than Vitest's 5s default). Lesson 34 uses `Expect<Equal<>>` and `@ts-expect-error` lines in solutions as type-level tests enforced by `pnpm typecheck`. Unverified-by-fetch: TS 7 perf multipliers (vendor figures); exact npm/Yarn minors; import-map scope keys use absolute URLs by design (stated in prompt).
