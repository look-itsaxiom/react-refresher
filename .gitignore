node_modules
# personal progress lives only on your machine (dev server writes it); static builds use localStorage
progress/
dist
*.local
.DS_Store
.superpowers
.authoring-quarantine
.claude/scheduled_tasks.lock
